# Account Model

This placeholder reserves the account model manual for the v1 setup package.
The completed document will define separate runtime, human admin, integration,
test, LDAP bind, and simulation operator accounts.

Until this manual is implemented, use `docs/prd.md`,
`docs/implementation-plan.md`, and `docs/reference-digest.md` as the authority.

# Jenkins Agent Setup Manual

This placeholder reserves the Jenkins SSH build-agent setup manual.

The completed manual will cover reviewed inputs, agent artifact staging,
runtime filesystem setup, Jenkins public-key handoff, SSH readiness,
validation, and evidence collection.

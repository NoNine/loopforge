jenkins:
  nodes:
    - permanent:
        name: "{{JENKINS_AGENT_NODE_NAME}}"
        remoteFS: "{{JENKINS_AGENT_REMOTE_FS}}"
        numExecutors: {{JENKINS_AGENT_EXECUTORS}}
        labelString: "{{JENKINS_AGENT_LABELS}}"
        launcher:
          ssh:
            host: "{{JENKINS_AGENT_HOST}}"
            port: {{JENKINS_AGENT_SSH_PORT}}
            credentialsId: "{{JENKINS_AGENT_CREDENTIAL_ID}}"
            sshHostKeyVerificationStrategy:
              knownHostsFileKeyVerificationStrategy:
                knownHostsFile: "{{JENKINS_AGENT_KNOWN_HOSTS_FILE}}"

# This node config is valid only after the reviewed known_hosts file is
# populated from an approved Jenkins agent host-key fingerprint.

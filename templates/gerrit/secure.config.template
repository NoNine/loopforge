# Gerrit secure config template.
#
# The rendered file is written from reviewed LDAP bind secret input and must
# not contain placeholder or redacted secret text.

[ldap]
	password = {{LDAP_BIND_PASSWORD}}
